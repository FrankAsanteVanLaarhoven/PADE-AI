[package]
name = "pade-kernel"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
authors = ["Frank Asante Van Laarhoven"]
description = "Deterministic admission, origin, and demonstration-value kernel."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
